@model CiuchApp.Domain.DropDownValueBase

<h4>Chcesz chcesz usunąć?</h4>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label class="control-label" for="Name">Nazwa</label>
                <input asp-for="Name" class="form-control" readonly="@(true)" />
            </div>
            @await Component.InvokeAsync("DeleteButtonRow")
        </form>
    </div>
</div>